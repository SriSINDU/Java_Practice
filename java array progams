//Java Program to copy all elements of one array into another array
import java.util.*;
 
public class CopyArray
{
  public static void main(String[] args)
  {
    int n;
    Scanner s = new Scanner(System.in);
    n= s.nextInt();
    int arr[] = new int[n];
    int arr1[] = new int[n];
    for(int i=0;i<n;i++){
      arr[i] = s.nextInt();                   // reading the array elements
    }
    for(int i=0;i<n;i++){
      arr1[i] = arr[i];                       // copying the 1st array elements into 2nd one by one
    }
    for (int i : arr1){
      System.out.println(i);                  // printing the second arrray elements
    }
   }
}   
